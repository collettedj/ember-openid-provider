// import ajaxPromise from '../../../utils/ajax-promise';
import { module, test } from 'qunit';

module('Unit | Utility | ajax promise');

// Replace this with your real tests.
test('it works', function(assert) {
  // let result = ajaxPromise();
  assert.ok(true);
});
